<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.sm.app.repository.ChatRepository">

    <insert id="insertMessage" parameterType="edu.sm.app.dto.ChatMessage">
        INSERT INTO chat_message (sender_id, receiver_id, content, is_read)
        VALUES (#{senderId}, #{receiverId}, #{content}, FALSE)
    </insert>

    <select id="getChatHistory" resultType="edu.sm.app.dto.ChatMessage">
        SELECT *
        FROM chat_message
        WHERE (sender_id = #{param1} AND receiver_id = #{param2})
           OR (sender_id = #{param2} AND receiver_id = #{param1})
        ORDER BY sent_at ASC
    </select>

    <select id="getChatPartners" resultType="java.lang.String">
        SELECT DISTINCT partner FROM (
            SELECT receiver_id AS partner FROM chat_message WHERE sender_id = #{userId}
            UNION
            SELECT sender_id AS partner FROM chat_message WHERE receiver_id = #{userId}
        ) AS partners
    </select>

</mapper>