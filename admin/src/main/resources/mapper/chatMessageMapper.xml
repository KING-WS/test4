<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="edu.sm.app.repository.ChatMessageRepository">

    <insert id="insert" parameterType="chatMessage">
        INSERT INTO chat_message (send_id, receive_id, content1, send_time)
        VALUES (#{sendId}, #{receiveId}, #{content1}, NOW())
    </insert>

    <select id="selectAllByUser" resultType="chatMessage">
        SELECT * FROM chat_message
        WHERE (send_id = #{param1} AND receive_id = #{param2})
           OR (send_id = #{param2} AND receive_id = #{param1})
        ORDER BY send_time ASC
    </select>

    <select id="selectAll" resultType="chatMessage">
        SELECT * FROM chat_message ORDER BY send_time DESC
    </select>

    <select id="select" parameterType="int" resultType="chatMessage">
        SELECT * FROM chat_message WHERE message_id = #{id}
    </select>

    <update id="update" parameterType="chatMessage">
        UPDATE chat_message SET content1 = #{content1} WHERE message_id = #{messageId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM chat_message WHERE message_id = #{id}
    </delete>

</mapper>